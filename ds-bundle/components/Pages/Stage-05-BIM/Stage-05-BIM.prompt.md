# Stage-05-BIM

05 BIM 協調 · 三層疊合圖解

這是 HQ 官網 `site/process/bim/index.html` 的預覽快照，用來展示完整的版面語彙與區塊節奏。原始檔為純靜態 HTML，只引用 `styles.css`。

## 這一頁示範的模式

btn、eyebrow、grid、wrap

## 注意

- 卡片內的站內連結已改為 `#`（Claude Design 內不跳轉）
- 影像為 640px 縮圖；正式站為 2560px
- 原始頁面的 canonical／OG 標記已移除
